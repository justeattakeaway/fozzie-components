const copy = {
    locale: 'en-IE',
    text: 'I am a VueSearchbox Component (IE)'
};

const component = {
    locationFormat: location => location.postcode
};

export default {
    copy,
    component
};
