/**
 * Callback service, empty for now, will be expanded upon
 * when we add the other services e.g. google places in the next few prs.
 *
 * @param Service
 */
export default Service => {
    Service();
};
