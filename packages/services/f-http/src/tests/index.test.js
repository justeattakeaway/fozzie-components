import HttpClient from '../httpClient';

describe('f-http', () => {
    beforeEach(() => {});

    it('httpClient should be defined', async () => {
        // Arrange & Act
        // Assert
        expect(HttpClient).toBeDefined();
    });
});
